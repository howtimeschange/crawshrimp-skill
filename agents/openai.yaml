interface:
  display_name: "Crawshrimp Skill"
  short_description: "AI agent CDP web operation protocol"
  default_prompt: "Use $crawshrimp-skill to operate the current webpage through the user's CDP 9222 browser, prefer page-owned API/request paths over manual clicking, and complete the workflow with evidence."
